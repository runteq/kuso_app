<div class="mb-3">
  <label class="form-label">お名前</label>
  <%= f.text_field :name, class: "form-control rounded-0" %>
  <%= full_error_message_on(f.object, :name) %>
</div>
<div class="mb-3">
  <label class="form-label">メールアドレス</label>
  <%= f.text_field :email, class: "form-control rounded-0" %>
  <%= full_error_message_on(f.object, :email) %>
</div>
<div class="mb-3">
  <label class="form-label">作品タイトル</label>
  <%= f.text_field :title, class: "form-control rounded-0" %>
  <%= full_error_message_on(f.object, :title) %>
</div>
<div class="mb-3">
  <label class="form-label">紹介文</label>
  <%= f.text_area :description, class: "form-control rounded-0", rows: 10 %>
  <%= full_error_message_on(f.object, :description) %>
</div>
<div class="mb-3">
  <label class="form-label">作品イメージ画像</label>
  <%= f.file_field :photo %>
  <%= full_error_message_on(f.object, :photo) %>
</div>
<div class="mb-3 text-center">
  <%= f.submit '応募', class: "btn btn-secondary btn-lg rounded-0 " %>
</div>
